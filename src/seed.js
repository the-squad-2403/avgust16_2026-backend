// TODO: seed script for local/dev database. Run with `node src/seed.js`.
//
// Needs to seed:
// - 2 LessonPaths x 5 lessons each (e.g. ru->en and uz->en paths)
// - 1 full dialog lesson (Lesson with type: 'dialog') within one of the paths
// - 20 vocab words per language pair (Vocabulary docs), linked to lessons
